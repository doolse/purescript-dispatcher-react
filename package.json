{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "pretest": "pulp build",
    "test": "pulp test --runtime cat > html/index.js"
  },
  "devDependencies": {
    "purescript-psa": "^0.5.1",
    "react": "^0.14.7",
    "react-dom": "^0.14.7",
    "rimraf": "^2.4.1"
  }
}
