{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "pretest": "pulp build -I test",
    "test": "pulp browserify -m Test.Main --to html/index.js"
  },
  "devDependencies": {
    "create-react-class": "^15.6.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.5.1",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "rimraf": "^2.4.1"
  }
}
